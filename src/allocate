allocate (xi(ndim_x))
allocate (yi(ndim_y))
allocate (zi(ndim_z))
allocate(ext_pot(ndim_x,ndim_y,ndim_z))
allocate(Vtot_dn(ndim_x,ndim_y,ndim_z))
allocate(Vtot_up(ndim_x,ndim_y,ndim_z))
allocate(rho_up(ndim_x,ndim_y,ndim_z))
allocate(exact_rho(ndim_x,ndim_y,ndim_z))
allocate(rho_up_old(ndim_x,ndim_y,ndim_z))
allocate (vx(ndim_x,ndim_y,ndim_z))
allocate (vh(ndim_x,ndim_y,ndim_z))
allocate(rho_dn(ndim_x,ndim_y,ndim_z))
allocate(rho_dn_old(ndim_x,ndim_y,ndim_z))
!allocate (vh_dn(ndim_x,ndim_y,ndim_z))
!allocate (igenE(NUME))
allocate (igenE_up(NUME))
allocate (igenE_dn(NUME))
allocate (fe(2,nume)) 
!allocate (degenaracy(nume))
allocate(eigen_vec_up(IWRSZ))   ! to see what is IWRSZ see dvd setup
allocate(eigen_vec_dn(IWRSZ))
allocate(hamilton_index(compressed_dim))
allocate (ke_hamilton_elements(compressed_dim))
